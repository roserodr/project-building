const fs = require('fs');
// Let's copy brown.dat, read the palette, modify the transparent mapping or something.
// Wait, skill icons use pal.dat which is unit palette.
// Maybe units/pal.dat is the unit palette? We don't have it.
